import { metadataForPublicPage } from "@/lib/seo";

export const metadata = metadataForPublicPage("/Divorced");

export default function DivorcedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
