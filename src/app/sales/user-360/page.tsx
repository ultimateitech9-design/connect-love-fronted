"use client";

import { User360ReadOnly } from "@/components/management/User360ReadOnly";

export default function SalesUser360Page() {
  return (
    <User360ReadOnly
      title="User 360"
      subtitle="Read-only subscriber profile, plan, and account status view."
    />
  );
}
